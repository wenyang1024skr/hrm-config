eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1010/eureka/
  instance:
    instance-id: hrm-redis-service
    prefer-ip-address: true

server:
  port: 2040
spring:
  application:
    name: hrm-redis-service
